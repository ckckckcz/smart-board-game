# Supabase Configuration
# Dapatkan credentials dari: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Vercel Cron Security (Optional tapi recommended)
# Generate random string untuk keamanan cron endpoint
# Anda bisa generate dengan: openssl rand -hex 32
CRON_SECRET=your_random_secret_here
