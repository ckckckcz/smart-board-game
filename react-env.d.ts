/// <reference types="react" />
/// <reference types="react-dom" />

// Ensures JSX intrinsic element typings are available in all TS/TSX files
// even when the editor temporarily falls back to an inferred project.
